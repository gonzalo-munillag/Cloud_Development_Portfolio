apiVersion: v1
kind: Service
metadata:
  name: app-date-todos
  labels:
    service: app-date-todos
spec:
  ports:
  - port: 8080
    name: "8080"
    targetPort: 8080
  selector:
    service: app-date-todos